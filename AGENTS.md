# Repository instructions

- This repository preserves the legacy, dependency-free static prototype that preceded We Remember.
- Keep the existing HTML/CSS/JavaScript and SVG structure; do not add frameworks, packages, build systems, or new product features.
- Archive only the early prototype, initial PRD, assets, and concise public-safe hackathon history.
- Do not import post-hackathon code, uncommitted worktree files, agent logs, credentials, or private evidence.
- Use relative links, validate representative HTML and local asset references, run `git diff --check`, and report limitations truthfully.
- Do not archive, rename, merge, release, or modify the successor repository from this worktree.
