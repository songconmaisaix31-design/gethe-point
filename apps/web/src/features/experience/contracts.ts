// Root dependency declarations are foundation-owned, so this single bridge keeps
// the experience track on the frozen contract source without changing the lockfile.
export * from "../../../../../packages/contracts/src/index";
